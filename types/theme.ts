export interface ThemeColors {
  background: {
    primary: string;
    secondary: string;
  };
  neon: {
    primary: string;
    secondary: string;
    accent: string;
  };
  text: {
    primary: string;
    secondary: string;
  };
  gradient: {
    hero: string;
    section: string;
  };
}
